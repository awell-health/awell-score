// import { is_string } from '../../../../../utils/validation/is_string'
// import { try_cast_to_string } from './try_cast_to_string'

// export const try_cast_to_strings_array = (orginal_value: unknown): unknown => {
//   if (Array.isArray(orginal_value)) {
//     if (orginal_value.every(is_string)) return orginal_value

//     return orginal_value.map(try_cast_to_string)
//   }

//   return orginal_value
// }
