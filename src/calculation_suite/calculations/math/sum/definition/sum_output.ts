import type { CalculationOutputDefinition } from '../../../../../types/calculations.types'

export const SUM_OUTPUT: CalculationOutputDefinition[] = [
  {
    subresult_id: 'SUM',
    label: { en: 'Sum' },
    type: 'number',
  },
]
