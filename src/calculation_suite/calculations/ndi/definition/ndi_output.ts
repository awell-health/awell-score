import type { CalculationOutputDefinition } from '../../../../types/calculations.types'

export const NDI_OUTPUT: CalculationOutputDefinition[] = [
  {
    subresult_id: 'NDI',
    label: { en: 'NDI Score' },
    type: 'number',
  },
]
