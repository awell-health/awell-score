import type { CalculationOutputDefinition } from '../../../../types/calculations.types'

export const SST_OUTPUT: CalculationOutputDefinition[] = [
  {
    subresult_id: 'SST',
    label: { en: 'Total SST score' },
    type: 'number',
  },
]
