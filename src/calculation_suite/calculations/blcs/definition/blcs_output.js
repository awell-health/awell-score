// @flow

import type { CalculationOutputDefinition } from '../../../../types/calculations.types'

export const BLCS_OUTPUT: CalculationOutputDefinition[] = [
  {
    subresult_id: 'BLCS_TOTAL_SCORE',
    label: { en: 'Total score' },
    type: 'number'
  }
]
